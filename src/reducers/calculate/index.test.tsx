describe('Calculate reducer', ()=> {
    // FIXME the way I have it structured right now somewhere store.getState() needs to be called,
    // and I don't know how to make that work in a test environment, so as of now I don't know how to test this.
    // With properly structured reducers getState() shouldn't need to be called I think.
    it('Should calculate draw probability', () => {

    });
    it('Should calculate ping probability', () => {

    });
});